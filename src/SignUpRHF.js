import { useForm } from "react-hook-form";
import { yupResolver } from "@hookform/resolvers/yup";
import * as Yup from "yup";

export default function SignUpRHF() {
  // Challenge
  // Refactor SignUpFormik in order to use RHF, all needed dependencies are installed
  // If you are feeling lost:
  // 0. Go to https://react-hook-form.com/get-started
  // 1. Bring here all logic and boilerplate you can reuse from SignUpFormik
  // 2. Use useForm to create an instance of RHF, and register the input fields
  // 3. Make use of watch or useWatch to get values from RHF state

  return (
    <>
      <div>Code your awesome RHF here!</div>
    </>
  );
}
